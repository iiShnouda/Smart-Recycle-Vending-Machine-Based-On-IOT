# Copy this file to .env and fill in real values.

# From Atlas → Database → Connect → Drivers → "Python"
MONGO_URI=mongodb+srv://ahmede01602_db_user:YOUR_DB_PASSWORD@cluster0.tonq9xn.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Pick any long random string. The kiosk app must use the same value.
BACKEND_API_KEY=replace-with-a-long-random-string

# Database name inside the cluster
MONGO_DB=rewingo
