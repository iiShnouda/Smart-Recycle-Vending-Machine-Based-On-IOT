import QtQuick

/*
 *  Hairline — single thin horizontal rule in the gold tone.
 *  Use it as a divider between editorial sections.
 */
Rectangle {
    height: 1
    color: "#D4A574"
    opacity: 0.55
}
