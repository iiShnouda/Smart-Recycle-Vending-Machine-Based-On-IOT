# ReWinGo kiosk backend — copy to ".env" and fill in MONGODB_URI.
MQTT_HOST=f5248f86bfed44398ce41cdf27760831.s1.eu.hivemq.cloud
MQTT_PORT=8883
MQTT_USERNAME=ReWinGo
MQTT_PASSWORD=your-hivemq-password

# From MongoDB Atlas → Cluster0 → Connect → Drivers:
#   mongodb+srv://USER:PASS@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
# NOTE: if your network blocks SRV/TXT DNS lookups (some phone hotspots do),
# the mongodb+srv:// form times out (queryTxt ETIMEOUT). Use the STANDARD
# seed-list URI instead — it resolves the shard hosts via plain A-records:
#   mongodb://USER:PASS@h1:27017,h2:27017,h3:27017/?ssl=true&replicaSet=atlas-xxxx-shard-0&authSource=admin&retryWrites=true&w=majority
# (get the shard hosts + replicaSet from Atlas, or resolve _mongodb._tcp.<cluster> over DoH).
MONGODB_URI=
MONGODB_DB=rewingo

# Secret for signing the app's login JWTs (/api/auth). Long random string.
JWT_SECRET=change-me-to-a-long-random-string

# PORT is provided automatically on Railway — only set it for local runs.
PORT=3001

# ── Deploying on Railway (recommended for the grad-project demo) ──────────────
# Run THIS backend on Railway so it's public; then the Pi (on the iPhone
# hotspot) only needs HiveMQ, never an Atlas SRV lookup. Steps:
#   1. New Railway project → Deploy from repo → root = iot/kiosk-backend
#   2. Add the Variables above (MQTT_*, MONGODB_URI [srv form is fine on
#      Railway], MONGODB_DB, JWT_SECRET). Do NOT set PORT.
#   3. Point the mobile app's BackendConfig.baseUrl at the Railway URL.
